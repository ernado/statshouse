reference_target_config:
  query_url: http://localhost:9090

test_target_config:
  query_url: http://localhost:10888/prom/

query_tweaks:
  - note: Align timestamps to step, truncate timestamps
    truncate_timestamps_to_ms: 1000
    align_timestamps_to_step: true
